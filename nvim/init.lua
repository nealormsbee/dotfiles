-- Base configs
require("cfg") -- basic configs
require("remap") -- basic keymaps
require("pack") -- VimPack (plugin installs)
require("colors") -- set colorscheme after installing plugins

-- Plugin configs
require("lsp")
